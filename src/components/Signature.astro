---
/**
 * TODO(signature): replace this with an SVG scan of Malika's real handwriting.
 * Drop the file at src/assets/signature.svg, import it, and render it in place
 * of .signature__name — then remove Caveat from scripts/font-manifest.mjs and
 * the --hand token from tokens.css.
 *
 * Until then this is a placeholder set in Caveat. Caveat has NO U+02BB, so this
 * element may only ever contain the word "Malika" — never Uzbek text.
 * (scripts/font-manifest.mjs subsets the face to exactly those five glyphs.)
 */
---

<p class="signature">
  <span class="signature__rule" aria-hidden="true"></span>
  <span class="signature__name">Malika</span>
</p>
