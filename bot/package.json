{
  "name": "malika-bot",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram bot that writes content into the same Git repository Keystatic writes to.",
  "scripts": {
    "start": "node src/index.mjs"
  }
}
